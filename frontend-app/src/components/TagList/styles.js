const styles = ({ spacing }) => ({
  root: {
    display: 'flex',
    justifyContent: 'left',
    flexWrap: 'wrap',
    padding: spacing.unit * 2.5,
  },

  chip: {
    margin: spacing.unit / 2,
  },
});

export default styles;
